"""Environment: continuous scalar fields + discrete entities on a shared grid.

A4 non-stationarity is a set of dynamics toggled on the *same* World — a subclass
of behaviour, not a rewrite (§10).
"""
