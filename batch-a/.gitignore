# Simulation outputs (reproducible from code+config+seed; do not commit)
runs/
experiments/results/

# Stray figure output — `python -m viz.figures.X public/figures` run from batch-a/
# resolves the relative path here instead of the repo-root public/. Canonical
# figures are written by `python -m viz.build` to repo-root public/figures.
public/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
